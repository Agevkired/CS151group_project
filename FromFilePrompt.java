import javax.swing.JFrame;

/**
 * Prompts the user to enter the file location, loads file, and then adds
 * file to model. Part of controller.
 * @author
 *
 */
public class FromFilePrompt extends JFrame // a guess to what it should extend.
{
	// Extra classes may need to be defined because the prompt can be complex.
	
	// FromFilePrompt probably should have a reference to the model. This can be added in main.
	
	// Closing this frame SHOULD NOT close program.
}
